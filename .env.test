# Valor FICTICIO para los tests: garantiza que la clave compartida está
# activa de forma determinista en cualquier entorno (local y CI), sin depender
# de .env.local (que no existe en CI). Ver src/lib/ai/sharedKey.ts.
VITE_AI_SHARED_GEMINI_KEY=test-shared-gemini-key

/**
 * Adaptador del anejo para Cuadro de materiales (`concreta-materiales`).
 * Ver `../types.ts` y `../adaptador.ts`.
 */

import { definirAdaptador } from '../adaptador';

export const materialesAnejo = definirAdaptador({
  modulo: 'concreta-materiales',
  seccion: 'memoria',
  capitulo: 'Cuadro de materiales',
});

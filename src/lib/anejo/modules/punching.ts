/**
 * Adaptador del anejo para Punzonamiento (`concreta-punching`).
 * Ver `../types.ts` y `../adaptador.ts`.
 */

import { definirAdaptador } from '../adaptador';

export const punchingAnejo = definirAdaptador({
  modulo: 'concreta-punching',
  seccion: 'piezas',
  capitulo: 'Punzonamiento',
});

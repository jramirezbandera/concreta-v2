/**
 * Adaptador del anejo para Pilares de madera (`concreta-timber-columns`).
 * Ver `../types.ts` y `../adaptador.ts`.
 */

import { definirAdaptador } from '../adaptador';

export const timberColumnsAnejo = definirAdaptador({
  modulo: 'concreta-timber-columns',
  seccion: 'piezas',
  capitulo: 'Pilares de madera',
});

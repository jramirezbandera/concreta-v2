/**
 * Adaptador del anejo para Forjados (`concreta-forjados`).
 * Ver `../types.ts` y `../adaptador.ts`.
 */

import { definirAdaptador } from '../adaptador';

export const forjadosAnejo = definirAdaptador({
  modulo: 'concreta-forjados',
  seccion: 'piezas',
  capitulo: 'Forjados',
});

/**
 * Adaptador del anejo para Placas de anclaje (`concreta-anchor-plate`).
 * Ver `../types.ts` y `../adaptador.ts`.
 */

import { definirAdaptador } from '../adaptador';

export const anchorPlateAnejo = definirAdaptador({
  modulo: 'concreta-anchor-plate',
  seccion: 'piezas',
  capitulo: 'Placas de anclaje',
});

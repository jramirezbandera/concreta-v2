/**
 * Adaptador del anejo para Encepados (`concreta-pile-cap`).
 * Ver `../types.ts` y `../adaptador.ts`.
 */

import { definirAdaptador } from '../adaptador';

export const pileCapAnejo = definirAdaptador({
  modulo: 'concreta-pile-cap',
  seccion: 'piezas',
  capitulo: 'Encepados',
});

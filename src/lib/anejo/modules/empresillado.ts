/**
 * Adaptador del anejo para Empresillado (`concreta-empresillado`).
 * Ver `../types.ts` y `../adaptador.ts`.
 */

import { definirAdaptador } from '../adaptador';

export const empresilladoAnejo = definirAdaptador({
  modulo: 'concreta-empresillado',
  seccion: 'piezas',
  capitulo: 'Empresillado',
});

/**
 * Adaptador del anejo para Viento y nieve (`concreta-viento-nieve`).
 * Ver `../types.ts` y `../adaptador.ts`.
 */

import { definirAdaptador } from '../adaptador';

export const vientoNieveAnejo = definirAdaptador({
  modulo: 'concreta-viento-nieve',
  seccion: 'memoria',
  capitulo: 'Viento y nieve',
});

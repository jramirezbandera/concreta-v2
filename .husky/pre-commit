#!/usr/bin/env sh
# Rápido a propósito: lint entero (es barato) y sólo las pruebas que tocan lo
# que estás commiteando. La suíte completa —7.702 pruebas, ~78 s— pasó a
# `pre-push` el 2026-09-12: pagarla en cada commit invitaba a `--no-verify`, y
# una prueba lenta que se pasa del timeout bajo carga tumbaba commits sanos.
bun run lint && bun run test:changed
